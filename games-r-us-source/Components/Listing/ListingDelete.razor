@using Microsoft.EntityFrameworkCore
@using games_r_us_source.Data
@inject IDbContextFactory<ApplicationDbContext> ContextFactory
@inject NavigationManager NavigationManager

@if (!IsListingDeleted)
{
    <button @onclick="ShowConfirmationDialog" class="rounded bg-red-500 px-4 py-2 text-white hover:bg-red-600">Delete</button>
}
else
{
    <div class="mt-4 text-green-500">Listing successfully deleted</div>
}

@if (ConfirmationDialog)
{
    <section class="fixed inset-0 flex items-center justify-center bg-black bg-opacity-50">
        <section class="rounded bg-white p-4">
            <p class="mb-4">Are you sure you want to delete this listing?</p>
            <div class="flex justify-center space-x-4">
                <button class="rounded bg-red-500 px-4 py-2 text-white" @onclick="RemoveListing">Yes</button>
                <button class="rounded bg-green-500 px-4 py-2 text-white" @onclick="() => ConfirmationDialog = false">No</button>
            </div>
        </section>
    </section>
}

@code {
    [Parameter]
    public int ListingID { get; set; }

    private bool IsListingDeleted { get; set; } = false;
    private bool ConfirmationDialog { get; set; } = false;

    private async Task ShowConfirmationDialog()
    {
        ConfirmationDialog = true;
    }

    private async Task RemoveListing()
    {
        using (var dbContext = ContextFactory.CreateDbContext())
        {
            dbContext.Listings.Remove(new Listing { ID = ListingID });
            await dbContext.SaveChangesAsync();
            IsListingDeleted = true;
            ConfirmationDialog = false;
            NavigationManager.NavigateTo("/", forceLoad: true);
        }
    }
}


